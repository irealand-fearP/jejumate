# 코덱스 단계별 작업 프롬프트 (2026-07-08 대표 작성)

사용법: 아래 프롬프트를 **순서대로 하나씩** 코덱스에 복붙. 한 단계가 끝나고 커밋+HANDOFF 갱신까지 확인한 뒤 다음 단계로. 토큰이 끊겨도 각 단계가 독립적으로 완결되게 설계했으니 중간에 멈춰도 이어받기 쉽다.

---

## 0단계 (매 세션 시작 시 — 새 코덱스 세션마다 맨 앞에 붙이기)

```
C:\Users\AI융합원\jejumate-work\jejumate-fork 에서 작업한다. 시작 전에 CODEX-HANDOFF.md의 "최신 전달사항"을 읽어라.

지켜야 할 규칙:
- API 규약 변경 금지: 신청 ID는 application_id, owner_secret은 쿼리 파라미터, 에러는 403(코드 불일치)/409(정원 초과)/400(중복 처리), RAG threshold 0.5, 근거 없으면 sources 빈 배열(임의 문서 반환 금지)
- .env 값·DATABASE_URL·INGEST_SECRET·API 키는 절대 출력·커밋 금지
- git add -A 금지, 본인이 만진 파일만 add. push는 하지 말 것(로컬 커밋까지만)
- 작업 완료 기준: apps/web에서 npm run typecheck 통과 + services/api에서 python -m compileall app 통과 + 실동작 확인
- 끝나면 CODEX-HANDOFF.md "최신 전달사항"에 [변경 요약 / 검증 결과 / 남은 일]을 갱신하고 커밋
```

---

## 1단계: 카톡 수집 파이프라인 배포 연동 (최우선)

```
[0단계 규칙 적용]

카톡 실시간 수집→RAG 파이프라인(커밋 35c000e, services/api/app/api/routes/ingest.py)은 로컬에서 검증 완료 상태다. 이걸 배포판에서도 돌게 만들어라.

1. GET /api/ingest/kakao 트리거 엔드포인트가 배포 환경(Vercel 서버리스)에서 동작하는지 확인. INGEST_SECRET 쿼리 파라미터로 보호돼 있다 — Vercel 환경변수에 INGEST_SECRET 등록(값은 새로 생성, 출력 금지).
2. 수집 커서(마지막 처리한 after_id)가 배포 DB(Postgres)에 저장돼 서버리스 인스턴스가 바뀌어도 유지되는지 확인.
3. 주기 실행: vercel.json의 crons 설정으로 5분 간격 /api/ingest/kakao 호출을 등록하라. Vercel Hobby 플랜은 cron이 하루 1회 제한일 수 있으니, 제한에 걸리면 대안(cron-job.org 같은 외부 무료 크론에 시크릿 포함 URL 등록)을 적용하고 어떤 방식을 썼는지 HANDOFF에 기록.
4. 검증: 실제 수집 API(https://dm.kggstudio.com/chats?after_id=)에서 새 메시지를 가져와 배포 DB rag_documents에 적재되고, 배포판 /api/rag/ask 질문 응답에 반영되는 것까지 확인.
5. 중복 방지 확인: 같은 트리거를 두 번 연속 호출해도 같은 메시지가 이중 적재되지 않아야 한다.
```

---

## 2단계: 배포판 전체 흐름 실클릭 검증 + 발견 버그 즉시 수정

```
[0단계 규칙 적용]

배포판 https://jejumate-web.vercel.app 을 실사용자처럼 브라우저로 끝까지 검증하고, 발견되는 버그는 그 자리에서 수정·재배포하라. 구조 확인이 아니라 실제 클릭으로:

1. 모임 등록(관리코드 발급) → 다른 브라우저 프로필에서 신청 → 호스트로 승인 → 승인 후 상태 표시까지. 몇 분 간격을 두고 재접속해서 데이터가 사라지지 않는지(신청 유실 재발 여부) 확인.
2. 채팅 노출 규칙: 신청만 한 상태에서는 채팅이 안 보여야 하고, 승인되면 보여야 한다. 호스트 화면도 확인.
3. 내정보: '승인된 모임'과 '내가 만든 모임'이 각각 올바른 목록으로 나뉘어 보이는지.
4. 홈 필터 버튼 5개 → 모임 화면에서 실제 필터링되는지.
5. RAG 질문 2건: "오전에 제주공항에서 출발하는 택시팟" 류(매칭 기대)와 전혀 무관한 질문(sources 빈 배열 기대).
6. 생활게시판 글쓰기→댓글→본인 삭제→신고 회귀.
7. 모바일 화면(개발자도구 모바일 뷰포트 + 가능하면 실기기)에서 위 흐름 재확인.

원칙: 화면에 보이는 버튼은 전부 실제로 작동해야 한다(숨김 처리 금지 — 소유자 방침). 작동 안 하는 게 나오면 기능을 구현해서 해결하라.
```

---

## 3단계: 카톡 메시지 → 콜드스타트 피드 자동화

```
[0단계 규칙 적용]

수집된 카톡 메시지를 RAG 적재에서 한 단계 더 발전시켜, 서비스 콘텐츠로 자동 변환하는 기능을 만들어라. 제품 방향: 카톡 데이터는 초반 유입(콜드스타트)용이고, 유입이 생기면 서비스 내 작성 글로 대체된다.

1. 수집 메시지 분류: 동행/이동 구인(예: "내일 1시 애월 가실 분", "7/9 오설록 다녀올 2분 구합니다") / 중고·나눔 / 질문 / 기타 잡담. OpenAI 호출로 분류하되 비용 절약을 위해 노이즈 필터 통과분만 분류.
2. 동행 구인 글 → 모임 후보로 변환해 모임 목록에 노출. 반드시 '오픈채팅에서 온 글' 배지로 서비스 내 작성 글과 구분하고, 원문·시간 정보 보존. 신청/승인 흐름은 붙이지 말 것(외부 글이라 호스트가 없음) — 대신 "오픈채팅에서 참여하기" 정도의 안내로 처리.
3. 중고·나눔·질문 → 생활게시판 해당 카테고리에 같은 배지로 노출.
4. 안전장치: 같은 메시지 중복 변환 금지, 시간이 지난 글(모임 시각 경과) 자동 숨김, 하루 자동 생성 상한(예: 30건), 전화번호·URL 등 개인정보 패턴 마스킹.
5. 검증: 실제 수집 데이터로 변환 결과 10건을 뽑아 분류가 맞는지 표로 정리해 HANDOFF에 기록.
```

---

## 4단계: RAG 데이터 품질·시드 균형

```
[0단계 규칙 적용]

RAG 품질 개선. threshold 0.5는 유지하고 데이터만 손본다.

1. 현재 rag_documents 카테고리 분포를 뽑아라(ride 편중 상태일 것). 맛집/숙소/생활 카테고리가 부족하면 수집 데이터에서 해당 유형을 더 적극적으로 적재하도록 노이즈 필터 기준을 조정.
2. 노이즈 필터 튜닝: 지금 기준(이모지만/10자 미만/단순 답장 제외)이 유효한 정보를 버리거나 잡담을 통과시키는 사례를 실데이터 50건으로 점검하고 조정.
3. 스팟체크: 카테고리별 질문 10개(이동 3, 맛집 3, 숙소 2, 생활 2)를 배포판에 던져 [질문/답변 요지/sources 수/판정]을 표로 HANDOFF에 기록. 무관 질문 2개도 포함해 회피가 유지되는지 확인.
4. 오답이나 이상 응답이 나오면 원인(데이터 부족 vs 필터 문제 vs 프롬프트)을 구분해 고치되, threshold와 "근거 없으면 빈 배열" 규약은 절대 건드리지 말 것.
```

---

## 5단계: 데모 시나리오·마감 점검

```
[0단계 규칙 적용]

발표 데모 준비를 도와라 (발표자료 본체는 별도 작업이니 만들지 말 것).

1. 데모 시나리오 스크립트를 docs/demo-scenario.md 로 작성: ①홈 소개 ②모임 등록→신청→승인→채팅 ③RAG 질문(잘 되는 질문 2개: 택시팟/함덕, 회피 질문 1개) ④라이브 장면 — 오픈채팅방에 메시지 올리고 몇 분 뒤 서비스에 반영되는 흐름(1·3단계 결과 활용, 반영 소요 시간을 실측해서 적어라).
2. 시연용 데이터 정리: 배포 DB에 테스트 쓰레기 데이터(QA·개발 중 생성분)가 보이면 정리하고, 데모에 보기 좋은 모임 4~6개가 홈에 뜨도록 시드 보강.
3. 배포판 최종 스모크(홈/모임/질문/게시판/내정보 각 1회 클릭)와 로딩 속도 체감 점검. 느린 페이지가 있으면 원인만 HANDOFF에 기록(수정은 판단껏).
4. README.md를 현재 아키텍처(모노레포, Next.js 웹 + FastAPI + Supabase Postgres + 카톡 수집 파이프라인) 기준으로 갱신.
```

---

## 이어받기 (코덱스 작업 후 Claude 재개 시 — 사용자 메모)

Claude 세션에 "제주메이트 이어서, 코덱스 작업분 검수부터"라고 하면 된다. Claude는 windows fork와 WSL fork를 diff해서 코덱스 작업을 흡수·검수하는 절차를 안다.
